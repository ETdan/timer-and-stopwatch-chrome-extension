// function saveActivePage(page) {
//   chrome.storage.local.set({ lastPage: page });
// }

// function loadActivePage() {
//   chrome.storage.local.get(["lastPage"], function (result) {
//     const lastPage = result.lastPage || "defaultPage.html";
//     window.location.href = lastPage;
//   });
// }

// // Call this function when the extension is opened
// document.addEventListener("DOMContentLoaded", loadActivePage);
